{
  "scripts": {
    "compile": "truffle compile",
    "geth": "geth --testnet --rpc --rpcport 8546 console 2>> geth-test.log",
    "geth-live": "geth --rpc --rpcport 8547 console 2>> geth.log",
    "deploy": "truffle migrate --network test --reset",
    "deploy-live": "truffle migrate --network live --reset",
    "test": "sh scripts/test.sh",
    "lint": "solium --dir ."
  },
  "dependencies": {
    "zeppelin-solidity": "^1.0.7"
  }
}
